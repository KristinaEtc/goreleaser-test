package goreleaser

/*
// Info of the repository
type Info struct {
	Description string
	Homepage    string
	URL         string
}

// Client interface
type Client interface {
	CreateRelease(ctx *Context, body string) (releaseID int, err error)
	CreateFile(ctx *Context, content bytes.Buffer, path string) (err error)
	Upload(ctx *Context, releaseID int, name string, file *os.File) (err error)
}
*/
